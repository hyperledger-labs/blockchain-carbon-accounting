{
  "name": "@blockchain-carbon-accounting/methane",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run client:start\" \"npm run server:start\"",
    "prebuild": "tslint -p tsconfig.json --fix",
    "build": "cd clients/esg && npm run build && cd ../../server && npm run build && cd ..",
    "server": "nodemon --verbose --watch \"server\" --ext \"ts,json\" --exec ts-node server/server.ts",
    "server:start": "cd server && npm run start && cd ..",
    "client": "cd clients/esg && npm start && cd ../..",
    "client:start": "cd clients/esg && npm run start && cd ../..",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Bertrand Rioux: bertrand.rioux@gmail.com",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-request-context": "^1.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "@types/node": "^18.7.18",
    "concurrently": "^7.4.0",
    "nodemon": "^2.0.20",
    "react-app-rewired": "^2.2.1",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^4.8.3"
  }
}
